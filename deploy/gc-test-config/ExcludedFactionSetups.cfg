# EXCLUDED FACTION SETUP LIST : This list must use FactionSetup ID's, which are different from display names !
#
# Air Assault reduction. Removes the highest-impact AA battlegroup variants
# so each alliance loses roughly half its air-assault slots across the
# voting pools, keeping Republic and CIS in proportion to each other.
# Every faction keeps at least one AA variant, so no faction loses air
# assault entirely and no layer is left with an empty faction pool.
#
# In-line comments do NOT parse. Keep comments on their own line.
#

# ---- Republic ----
104P1_AirAssault_LD
125P2_AirAssault_MO
212P2_AirAssault_MO
241P2_AirAssault_LD
241P2_AirAssault_MD
41P1_AirAssault_MD
41P2_AirAssault_MD
42P1_AirAssault_MO
501P1_AirAssault_LO
501P2_AirAssault_MO
91P2_AirAssault_MD
CREBP1_AirAssault_LO
CREBP1_AirAssault_MD
GARP1_AirAssault_MO

# ---- CIS ----
DAC_AirAssault_MO
DAG_AirAssault_MO
DASO_AirAssault_LO
DASO_AirAssault_MD
DAS_AirAssault_MO

# ---- Empire ----
GEP3_AirAssault_LD
GEP3_AirAssault_MO
